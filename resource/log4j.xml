<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE log4j:configuration SYSTEM "log4j.dtd">
<log4j:configuration xmlns:log4j='http://jakarta.apache.org/log4j/'>

	<!-- Log4j提供的appender有以下几种: -->
	<!-- org.apache.log4j.ConsoleAppender(控制台), -->
	<!-- org.apache.log4j.FileAppender(文件), -->
	<!-- org.apache.log4j.DailyRollingFileAppender(每天产生一个日志文件), -->
	<!-- org.apache.log4j.RollingFileAppender(文件大小到达指定尺寸的时候产生一个新的文件), -->
	<!-- org.apache.log4j.WriterAppender(将日志信息以流格式发送到任意指定的地方) -->

	<!-- 控制台输出 -->
	<appender name="CONSOLE" class="org.apache.log4j.ConsoleAppender">
		<layout class="org.apache.log4j.PatternLayout">
			<param name="target" value="System.out" />
			<param name="ConversionPattern"
				value="%d{ISO8601} 日志内容：%m%n" />
		</layout>

		<!--限制输出级别 -->
		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="LevelMax" value="ERROR" />
			<param name="LevelMin" value="DEBUG" />
		</filter>
	</appender>

	<appender name="log4jTestDebug" class="org.apache.log4j.RollingFileAppender">
		<param name="File" value="D:/shop_log/debug.log" />
		<param name="Append" value="true" />
		<param name="MaxFileSize" value="5KB" />
		<param name="MaxBackupIndex" value="2" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern" value="%c %d{ISO8601}-- %p -- %m%n" />
		</layout>
		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="LevelMin" value="DEBUG" />
			<param name="LevelMax" value="DEBUG" />
		</filter>
	</appender>

	<!-- 当文件到达制定大小后生成一个新的文件 -->
	<appender name="log4jTestLogInfo" class="org.apache.log4j.RollingFileAppender">
		<param name="File" value="D:/shop_log/info.log" />
		<param name="Append" value="true" />
		<param name="MaxFileSize" value="5KB" />
		<param name="MaxBackupIndex" value="2" />
		<layout class="org.apache.log4j.PatternLayout">
			<param name="ConversionPattern" value="%c %d{ISO8601}-- %p -- %m%n" />
		</layout>
		<filter class="org.apache.log4j.varia.LevelRangeFilter">
			<param name="LevelMin" value="INFO" />
			<param name="LevelMax" value="INFO" />
		</filter>

	</appender>


	<!-- 通过<category></category>的定义可以将各个包中的类日志输出到不同的日志文件中 <category name="com.litt3"> 
		<level value="ERROR" /> <appender-ref ref="CONSOLE" /> </category> -->
	<!-- info log -->
	<!-- name指定的是包名，这样只操作此包下的log -->
	<logger name="com.zy.mapper" additivity="true">
		<level value="DEBUG" />
		<appender-ref ref="CONSOLE" />
	</logger>
	<logger name="com.zy.controller" additivity="true">
		<level value="INFO"></level>
		<appender-ref ref="CONSOLE" />
	</logger>

	<logger name="com.zy.service" additivity="true">
		<level value="ERROR"></level>
		<appender-ref ref="CONSOLE" />
	</logger>

	<!-- Root Logger -->
	<!-- 所有logger的父类，记录所有的日志。 -->
	<root>
		<level value="ALL"></level>  <!-- 限定记录等级 -->
		<appender-ref ref="log4jTestLogInfo" />  <!-- 调用记录方式 -->
	</root>
</log4j:configuration>