<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.a2.ssm.mapper.BrandMapper" >
  <resultMap id="brandResultMap" type="com.a2.ssm.model.Brand" >
    <id column="b_id" property="bId" jdbcType="INTEGER" />
    <result column="b_name" property="bName" jdbcType="VARCHAR" />
    <result column="pt_id" property="ptId" jdbcType="INTEGER" />
  </resultMap>
  
  	<!-- 查询全部Brand -->
  	<select id="list" resultMap="brandResultMap">
  		select * from t_brand where 1=1
  	</select>
	
	<!-- 根据id查询品牌信息  -->
	<select id="getById" parameterType="int" resultMap="brandResultMap">
		select * from t_brand where b_id=#{bId}
	
	</select>
	
	<!-- 更新品牌信息 -->
	
	<!-- 删除品牌信息 -->

	<!-- 添加品牌 -->
	
	<!--根据商品类型ID查询品牌信息  -->
	<select id="getByTypeId" parameterType="int" resultMap="brandResultMap">
		select * from t_brand where pt_id=#{id}
	</select>
  
</mapper>